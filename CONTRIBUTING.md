# Contributing

欢迎提交问题和小范围改进。请先说明可复现的问题、使用版本和预期行为；财务口径变更需要明确计算假设。

- 使用虚构账单、商户、产品和金额复现。不要提交真实 Excel、资产截图、数据库、备份或包含这些内容的日志。
- 个性化签名放在被忽略的 `Config/Local.xcconfig`。修改 `project.yml` 后重新运行 `Scripts/generate-project.sh`，不要提交生成的 Xcode 工程或个人 Team ID。
- 修改纯计算时运行相关 Swift 测试；改动导入、持久化、配对和界面时，运行对应 iOS 测试与构建。提交前可执行 `Scripts/verify.sh`。
- 目录数据必须注明可再分发的来源、版本和完整许可证。网页可访问不代表数据可重新打包发布。
- PR 简要写明行为变化、验证结果及尚未覆盖的场景。漏洞请走[私密报告入口](.github/SECURITY.md)。

提交贡献表示你有权公开该内容，并同意以项目 MIT 许可证提供。第三方内容须保留原许可声明。
